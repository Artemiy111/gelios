apiVersion: v1
kind: Service
metadata:
  name: {{ include "y.fullname" . }}
  labels:
    {{- include "y.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "y.selectorLabels" . | nindent 4 }}
